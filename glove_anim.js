import { correctCounter } from "./keyboard.js";

export function gloveAnim(lalala) {
  if (correctCounter === 1) {
    lalala.classlist.add('anim');
  }
  if (correctCounter === 2) {
    lalala.classlist.add('anim');
  }
  if (correctCounter === 3) {
    lalala.classlist.add('anim');
  }
  if (correctCounter === 4) {
    lalala.classlist.add('anim');
  }
  if (correctCounter === 5) {
    lalala.classlist.add('anim');
  }
  if (correctCounter === 6) {
    lalala.classlist.add('anim');
  }
}
